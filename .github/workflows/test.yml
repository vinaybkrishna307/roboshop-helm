name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check repos
        run: echo "$github.repository"

      - name: Check steps
        run: echo "$steps"
